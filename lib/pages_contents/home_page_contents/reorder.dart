import 'package:flutter/material.dart';

class ReOrder extends StatelessWidget {
  const ReOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(child: Container(color: Colors.deepPurple,));
  }
}
